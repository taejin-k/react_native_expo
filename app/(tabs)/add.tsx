import Wrapper from '@/components/common/Wrapper';
import { Text } from 'react-native';

const Add = () => {
  return (
    <Wrapper>
      <Text>Add Page</Text>
    </Wrapper>
  );
};

export default Add;
